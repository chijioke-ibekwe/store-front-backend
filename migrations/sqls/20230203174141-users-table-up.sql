CREATE TABLE users (id SERIAL PRIMARY KEY, first_name VARCHAR(100), last_name VARCHAR(100), username VARCHAR(100) NOT NULL UNIQUE, 
password VARCHAR(255), phone_number VARCHAR(100) UNIQUE, role_id INTEGER REFERENCES roles(id), verified BOOLEAN DEFAULT false);